# Stage P Case 01 Prompt V2 / V4 failure analysis

The single frozen probe demonstrates a semantic-role failure, not a transport, constrained-decoding, encoding, or source-membership failure. The model copied all three candidate spans exactly but converted the integrated figurative transformation into three unresolved `REAL_WORLD_COMMITMENT` entries. It performed none of the three affirmative coverage checks and emitted no contained-creative entry.

The narrowest robust remediation is contract-level. The current output grammar asks for `coverage_decision` before the ledger exists and permits a real-world entry whose every semantic axis is unresolved. Prompt wording alone would leave both escape paths valid.

The proposed `STAGE_P_ROLE_COHERENCE_V1` candidate therefore makes only the necessary structural changes: move coverage after entries and receipts; enforce type-specific tuple coherence; reserve unresolved semantic axes for `UNRESOLVED_SCOPE`; define an operational factual-return test; and prevent clause-level fragmentation from destroying an integrated creative construction. It preserves exact source binding, immutable factual authority, the existing enum namespace and entry ceiling, exhaustive embedded-proposition review, fail-closed abstention, and the prohibition on Stage P admission/FSEM decisions.

The existing source-bound projector is not recommended because the failed output already satisfied exact source membership. Stage C must not be called on this indeterminate ledger.

Recommended next bounded step, requiring owner authorization: implement an evaluation-only role-coherence schema/grammar/prompt candidate and perform zero-inference invariant, identity, padding, request-construction, and constrained-trie compatibility verification. Stop before model inference.
