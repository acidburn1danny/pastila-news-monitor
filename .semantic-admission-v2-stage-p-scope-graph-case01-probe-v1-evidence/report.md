# Stage P Scope Graph Case 01 — single-probe result

Exactly one evaluation-only call completed. Transport, durable persistence, constrained terminal EOS, strict schema validation, and source-substring validation passed. The raw output remains quarantined.

Semantic acceptance failed. Instead of representing the commentary as an integrated contained-creative host, the output created one `REAL_WORLD_COMMITMENT` spanning the entire commentary, labeled it `STANDALONE` and `PRESUPPOSED`, and said the governed event survives. Its commitment reproduces the factual summary while `authority_support` is null. This passes the present deterministic membership check but violates the intended scope-graph behavior and the prompt's support-alignment instruction.

No retry, repair, selection, Case 10, Stage C, or proof rerun occurred. Recommended next step: design-only failure analysis of the whole-construction factual collapse and null-support inconsistency.
