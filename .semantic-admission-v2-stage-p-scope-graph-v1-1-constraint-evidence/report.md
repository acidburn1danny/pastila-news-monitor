# Scope Graph V1.1 Constraint — zero-inference evidence

Result: **PASS**. A null-support real-world entry can no longer emit `GOVERNED_EVENT`; the invalid enum is removed before emission. Supported entries retain both governed and new-unsupported choices, preserving partial-support cases.

All relevant creative, embedded, governed, unsupported, and partial-support terminal fixtures pass. No tokenizer, request, runner, provider, model, or inference action occurred.
