# Stage P Role Coherence V1 zero-inference candidate

The evaluation-only schema, character grammar, and prompt candidate implement the frozen Case 01 failure remediation. Coverage is emitted after the ledger and review receipt. Both schema and grammar reject incoherent type/axis combinations, reserve unresolved candidate axes for `UNRESOLVED_SCOPE`, and preserve fail-closed indeterminacy.

Prompt V1 defines the factual-return test and integrated-construction rule while preserving exhaustive review of embedded, presupposed, entailed, and necessarily implied real-world propositions. It contains no expected case label or owner outcome.

The frozen Case 01-shaped contained-creative fixture passes. Incoherent real-world and contained-creative fixtures fail. Explicit unresolved and mixed creative-plus-real-world fixtures behave as designed. Exact source membership, Unicode prompt construction, unpadded execution bytes, one-unit request construction, and a synthetic character-token constrained-trie terminal path pass.

The final combined zero-inference and adjacent non-regression suite passed all 30 tests. One earlier test invocation had 11 passes and one failure caused solely by the word “provider” in a no-execution docstring; the docstring was corrected and the complete suite passed. No WSL process, real tokenizer, model weights, provider, inference, projector, or Stage C was invoked.

Recommended next bounded step, requiring separate authorization: run a zero-inference real-tokenizer trie/profile compatibility check using the frozen local tokenizer without loading model weights. If it passes, construct and identity-bind a one-shot evaluation runner, then stop again before inference.
