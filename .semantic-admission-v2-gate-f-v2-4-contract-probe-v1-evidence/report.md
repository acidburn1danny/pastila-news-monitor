# Semantic Admission V2 — Gate F V2.4 two-case probe

## Outcome

The authorized two-call Gate F-only probe completed exactly once. Both constrained responses were valid JSON and were durably captured without executor failure, retry, repair, selection, Gate S, or runtime impact.

The probe did not pass its full acceptance contract.

## Case 01

Observed: exact `PASS` with no reasons.

This is a meaningful correction over Run 4. V2.4 did not literalize the contained hotel/room/transparency metaphor into unsupported emotion or intent. The mandatory positive anchor passed exactly.

## Case 10

Observed: `FAIL`, but with the wrong proposition classes:

- `FSEM_UNSUPPORTED_CAUSALITY` — decisive and spurious.
- `FSEM_UNSUPPORTED_MOTIVE_OR_INTENT` — decisive and spurious.
- `FSEM_UNSUPPORTED_LIFE_STAKES` — materially correct, but marked decisive instead of supporting.

Required by the frozen contract:

- `FSEM_CERTAINTY_MUTATION` — decisive.
- `FSEM_TIMING_MUTATION` — supporting.
- `FSEM_UNSUPPORTED_LIFE_STAKES` — supporting.

The response therefore rejected the unsafe commentary but did not identify its governed semantic structure. It also used factual-summary wording as a purported exact candidate span for its first reason, which violates candidate-span grounding.

## Conclusion

V2.4 demonstrates improved figurative-scope handling on the positive anchor, but the hard factual-return negative remains nonconformant. Conservative rejection is insufficient because wrong decisive reasons fail the acceptance contract and do not demonstrate recognition of certainty or timing mutation.

The full ten-case Gate F run is not eligible.

## Authority boundary

Both outputs remain quarantined evaluation evidence. No runtime, production, curriculum, or training authority is granted.

## Recommended next step

Perform a design-only residual failure analysis before changing the candidate. Focus narrowly on proposition extraction, exact candidate-span grounding, certainty/timing mutation recognition, and decisive-versus-supporting status discipline. Do not execute another probe or full run without a separately versioned candidate and authorization.
