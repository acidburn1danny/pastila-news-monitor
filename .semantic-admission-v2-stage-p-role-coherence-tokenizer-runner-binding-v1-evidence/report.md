# Stage P Role Coherence V1 tokenizer and runner binding

The frozen local tokenizer loaded once in WSL without loading model weights or the adapter. The known Mistral regex warning was emitted and preserved; tokenizer configuration was not changed.

The role-coherence grammar passed all 391 real-tokenizer transitions across a complete contained-creative ledger and an explicit indeterminate ledger. Both streams decoded exactly, reached a terminal constraint state, and admitted only EOS after completion. Vocabulary size was 131,072 and the trie contained 221,961 nodes.

A separately versioned runner wrapper is now source-bound to the role-coherence DFA, incremental tracker, callback controller, existing optimized trie, append-only lifecycle, and unchanged durable V3 base runner. The wrapper has not been executed. No model import, model or adapter load, inference, provider call, runner call, projector binding, or Stage C construction occurred.

Recommended next bounded step, requiring separate authorization: implement and zero-inference verify the durable host executor and a one-shot `HMCV1-SASC-01` probe binding for these exact identities. Stop again before inference.
