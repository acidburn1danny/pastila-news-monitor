# A1/A2 development pilot: successor execution and blind scoring

The historical protocol at `37846897e4ef69b9ab10aeaadf33a78de16c2098` remains unchanged. The successor authority is `docs/artifacts/editor-core-editorial-bridge-causal-execution-authority-v2.json`; its canonical identity is `9d1ece6eddc0c24e3d91a2e1c4f05f25f0dc36405e48fb78c872cdf9c8ffd019`. It grants six named slots only: A1 and A2, each at seeds 271828, 314159, and 161803; one run per slot, at most 12 optimizer steps and checkpoint step 12. It binds the R2 step-9 parent. It excludes holdout access, adjudication, parent selection, promotion, and release. A deliberate owner invocation is still required for each run; publication alone does not start anything.

The consuming shell checks the exact successor authority in its own published HEAD and verifies that the remote branch resolves to that HEAD. `BRIDGE_A1A2_OWNER_AUTHORIZED=1` is only a deliberate-invocation guard and cannot independently authorize a run. Every target also passes the physical zero-step check immediately before model load. Completed outputs may be observed by zero-step only in the five non-target slots; the target slot must remain empty. Any output collision or changed source fails closed. A new run or retry in a previously used slot requires a new successor decision.

For human scoring after the run outputs exist, the custodian creates seven canonical response files with no answer key or holdout, then uses `prepare_editor_core_bridge_blind_scoring.py` to create separate primary, reference, and sealed-map roots. The custodian runs `audit_prepared` to bind all packet bytes and identities to the sealed map before handoff. The primary root has 72 shuffled X/Y packets. The reference root has 24 R2 packets and stays inaccessible to the reviewer until 72 primary scores are complete and locked. The sealed map remains in custodian custody until both scoring phases close. The `serve_editor_core_bridge_blind_scoring.py` server binds to localhost and presents the same mouse-first form for every packet. The custodian starts it with a reviewer identifier, packet root, score root, and primary/reference phase; the reviewer receives only the browser URL. A random session token limits access to this local browser session. This is an operational identity binding to a designated reviewer, not a cryptographic proof of personhood.

The scoring recorder uses canonical JSON, exclusive file creation, packet-byte hashes, packet identities, reviewer identifier, and a phase closure manifest. It rejects duplicate records, substitution, truncation, extra files, and incomplete closure. The primary phase must be locked before reference handoff. The lock is tamper-evident when reverified; local read-only bits discourage accidental edits. It is not WORM storage and does not protect against a privileged custodian deliberately rewriting all local evidence. Preserve closed roots externally with their hashes for audit if live scoring occurs. No score records are created during fixture smoke.

The reviewer should score factual/epistemic safety first, then editorial quality and Romanian quality. An explicit note is required for a factual failure or indeterminate comparison. A semantic win is never computed from exact-string match or training loss. The causal pilot may support only a controlled synthetic development signal under its matched recipe. Naturalistic transfer or a development-parent change requires separate independent evaluation.
