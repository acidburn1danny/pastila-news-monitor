# R2 causal diagnostic execution authority v1

This successor defines exactly twelve development-research slots: four frozen
factorial arms and the three common seeds. Each slot has one distinct output
slug, one permitted terminal run, nine optimizer steps, and a mandatory fresh
zero-step before model load. Publication supplies technical binding; a separate
owner instruction remains required to consume any slot.

The supervisor creates all output roots fail-closed, exposes fixture and
12-slot zero-step modes, and has a real sequential execution path through the
network-isolated per-slot launcher. Both layers require the published authority
plus a separate owner authorization before any real execution.
R2 step-9 remains the parent and this authority cannot select a parent, access
historical holdouts, promote, or release.
