# Input Contracts

`CompositionInputBundle` records the episode, selection, blueprint, ten required upstream systems, approved ordered segments, mandatory stories, exclusions, and its semantic fingerprint. Dependencies carry canonical identity, semantic version, fingerprint, compatibility, mutation status, and normalized readiness.

Duplicate, missing, incompatible, mutated, blocked, or fingerprint-invalid dependencies cannot be silently accepted.
