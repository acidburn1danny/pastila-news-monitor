# Composition Pipeline

Validated Input → Compatibility → Constraints → Priorities → Segments → Beats → EpisodeArc → Transitions → Callbacks → Tone/Emphasis → Rhythm/Delivery → Traceability → Validation → Readiness → Plan.

Every stage is deterministic and reference-based. Missing or corrupted inputs stop planning. Plan conflicts remain visible with derived readiness. No stage generates wording.
