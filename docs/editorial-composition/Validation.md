# Validation

Validation covers identity/version, dependencies, fingerprints, canonical protection, membership, order, contiguity, beat dependencies, transitions, callbacks, priorities, tone, emphasis, rhythm, delivery, traceability, EpisodeArc, stabilization, generated-language flags, and readiness consistency.

Findings are immutable and structured. Input corruption prevents planning; plan conflicts remain in the plan and determine readiness.
