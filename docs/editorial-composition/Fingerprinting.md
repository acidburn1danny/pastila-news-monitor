# Fingerprinting

Semantic SHA-256 uses recursive canonical UTF-8 JSON. Meaningful segment, beat, ArcStep, binding, tone, transition, and callback order is preserved; unordered references normalize.

Timestamps, runtime identifiers, filesystem/storage paths, transient diagnostics, and self-fingerprint fields are excluded. Equivalent semantics hash identically; meaningful changes do not.
