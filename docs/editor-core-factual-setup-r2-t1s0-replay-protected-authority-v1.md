# T1/S0 Replay-Protected execution authority

This research authority enumerates exactly six arm/seed slots. Each slot permits at most one run with nine optimizer steps, requires an exact-tokenizer zero-step immediately before model load, and requires a distinct new empty output root. Retry is forbidden. The supervisor stops the complete program at the first failure.

The authority remains bound to runtime source commit `2aafeb6536b779ad4e5c7b0417c86188a56a6bc7`, R2 step-9, recipe S0, and the frozen evaluator. It grants no parent-selection, promotion, release, historical-holdout, VOICE, or CHIEF EDITOR authority.

Publication of this artifact does not execute the program. A separate owner authorization is required for the single real six-slot execution.
