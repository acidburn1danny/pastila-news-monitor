# Stage P Role Coherence V2 Case 01 probe

Exactly one evaluation-only Case 01 call completed. Transport, raw persistence, strict role-coherent schema, exact source membership, and terminal EOS all passed. There were no retries, repairs, selections, projector, Stage C, Case 10, or proof rerun.

The late grammar dead end is fixed, but Case 01 still fails semantically. The model emitted three `REAL_WORLD_COMMITMENT` entries by literalizing the linked money/shadow, employees/light, and hotel/room/transparency components. It then emitted a fourth entry covering the entire sentence as `CONTAINED_CREATIVE` and explicitly stated that it carries no real-world commitment. The ledger is therefore internally scope-duplicative and semantically contradictory despite being schema-valid and `COMPLETE`.

Case 01 required contained-creative coverage with zero unsupported real-world commitments. Acceptance is not demonstrated.

Recommended next step: design-only analysis of overlap and scope consistency. The narrow likely control is to prohibit a contained whole-construction entry from coexisting with component `REAL_WORLD_COMMITMENT` entries unless those components pass an explicit factual-return test and record a distinct necessary implication. Do not change the grammar or run another probe until that rule is specified and tested against mixed creative-plus-real-world counterexamples.
