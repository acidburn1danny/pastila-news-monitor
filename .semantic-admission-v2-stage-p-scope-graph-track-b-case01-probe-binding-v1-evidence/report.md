# Track-B exclusive Case 01 probe binding

Result: **PASS — constructed, not executed; Case 01 blocked**.

The binding selects only byte-frozen `HMCV1-SASC-01`, requires a new exclusive evidence root, and contains exactly one evaluator call site. It is identity-bound to the approved Track-B evaluator and Track-A durable executor. Constraint-liveness failure receives its distinct top-level reason and durable receipt.

No retry, repair, selection, Case 10, Stage C, or proof-rerun edge exists. Subprocess launch was forbidden during construction verification. No WSL, runner, tokenizer, model, provider, inference, or Case 01 call occurred.
