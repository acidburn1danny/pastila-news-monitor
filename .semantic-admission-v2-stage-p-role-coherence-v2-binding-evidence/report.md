# Stage P Role Coherence V2 one-shot binding

The V2 runner, candidate, evaluator, durable executor, and one-shot Case 01 probe are identity-bound to the role-conditioned constraint and frozen prompt/schema/model/authority bytes. Construction reproduced one request unit under the 240-second timeout and hard one-call ceiling.

The probe contains one capture path and no retry, repair, selection, projector, Stage C, Case 10, or proof-rerun edge. Executor construction verified all dependencies and created zero lifecycle events. No WSL, model, provider, runner, or inference call occurred.

Twenty-one focused binding and constraint tests passed. Recommended next step, requiring separate authorization: execute exactly one evaluation-only V2 Case 01 probe and stop after durable evidence and semantic evaluation.
