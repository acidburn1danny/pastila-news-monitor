# Stage P prompt V2 / V4 zero-inference binding

The prompt-only path is fully constructed without executing it. Frozen Case 01 bytes render to an exact unpadded 3,754-character prompt with SHA-256 `a9d6ef75b82436b14d5d553323f9b5edc468349f5d8bb8dbf873c74f01b4d44f`. The application authority preserved those bytes exactly in one request unit under the frozen 240-second timeout.

The separately versioned evaluator is bound to prompt V2, the existing Stage P grammar, Core V1.2 model identity, V4 phase-separated evidence receipts, the durable lifecycle reconciler, and constrained runner V3. The source-bound projector is explicitly absent.

The prepared one-shot runner selects only `HMCV1-SASC-01`, persists its request and identity binding before execution, permits exactly one evaluator capture, writes raw bytes before validation, and records schema and source-membership phases independently. It has no Stage C construction edge and no retry, repair, or selection path.

Seventeen focused tests passed. The first receipt-generation attempt lacked its target directory and is preserved as a zero-call preparation failure; the accepted construction receipt then passed. No WSL process, provider, model, or inference was invoked, and no durable lifecycle events were created.

The bounded next step requires fresh owner authorization for exactly one Stage-P-only Case 01 probe. The source projector, Stage C, Case 10, full proof, runtime, curriculum, and training remain unauthorized.
