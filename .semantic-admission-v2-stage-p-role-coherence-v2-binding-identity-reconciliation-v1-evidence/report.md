# Stage P Role-Coherence V2 binding identity reconciliation

The historical zero-inference binding manifest and identity `c00901f681153fc6b4529d1d4600629628615baac58da8310fa76f4c3353d94d` remain unchanged. Six of nine bindings reproduce byte-for-byte. Three recorded historical versions do not reproduce from reachable Git history or the audited repository content.

The current binding candidate, candidate implementation, and binding test originate at provider-neutral normalization commit `fb3b265264ef973aaf15b84fde27eaf87797bd6f`. Their Git blobs and current SHA-256 values are bound explicitly in the reconciliation receipt.

No byte-equivalence or semantic-equivalence claim is made. The unavailable bytes are not reconstructed or fabricated, and no historical hash is rebound.

This reconciliation grants no probe execution, additional probe, provider, model, projector, Stage C, inference, runtime, or production authority.
