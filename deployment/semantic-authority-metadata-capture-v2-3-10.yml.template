# INERT V2.3.10 TEMPLATE. Tokens may be materialized only after RFC-3161 precommit.
name: semantic-authority-metadata-capture-v2-3-10
on:
  schedule:
    - cron: '@SCHEDULE_CRON@'
permissions:
  contents: read
  id-token: write
  attestations: write
jobs:
  capture:
    if: github.repository_id == '1355263083' && github.repository == 'acidburn1danny/pastila-news-monitor' && github.run_attempt == '1' && github.event.schedule == '@SCHEDULE_CRON@'
    runs-on: ubuntu-24.04
    container: python@sha256:edf6433343f65f94707985869aeaafe8beadaeaee11c4bc02068fca52dce28dd
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: '@WORKFLOW_COMMIT@'
          persist-credentials: false
      - name: Fail closed until separately authorized activation materializes exact command
        run: python -m pastila_scout.semantic_authority_deployment_v2_3_10
