# Scope Graph V1.1 Contract and Prompt — zero-inference evidence

Result: **PASS**. The frozen Case 01 failure is now rejected deterministically because it claims `GOVERNED_EVENT` without authority support. Positive fixtures preserve integrated creative hosts, embedded supported facts, unsupported presuppositions, plain governed facts, plain unsupported claims, and partially supported new claims.

The prompt now makes candidate communicative force the exclusive inventory source and requires creative-host neutralization before a return is assigned to the same words. It does not require humor, creative language, or one segmentation in every case.

The existing V1 grammar was deliberately not changed in this step and is not claimed to fully enforce V1.1. No tokenizer, request, runner, model, provider, or inference action occurred.
